# WIP NOT WORKING YET!

See: https://github.com/justinmeiners/lisp-interpreter

It compiles and output warnings (see: error.txt). 

It runs on Wii and shows lisp errors and the prompt, but doesn't work!

I could make Mac OSX Apps. See: https://github.com/petersieg/lisp-interpreter
